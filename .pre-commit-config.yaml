repos:
  - repo: local
    hooks:
      # Check formatting (no auto-fix)
      - id: ruff-format
        name: ruff format (check only)
        entry: bash -c 'source .venv/bin/activate && ruff format --check'
        language: system
        types: [python]
        exclude: ^(tests/|stubs/|.agent/)
        verbose: true

      # Auto-fix linting issues
      - id: ruff-lint
        name: ruff lint (auto-fix)
        entry: bash -c 'source .venv/bin/activate && ruff check --fix --show-fixes'
        language: system
        types: [python]
        exclude: ^(tests/|stubs/|.agent/|.kiro/)
        verbose: true
